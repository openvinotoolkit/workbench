{% include 'autogenerated_note_code.jinja' %}


!{{ python_executor }} \
    -m "{{ model_xml_path }}" \
{% if has_tokenizer_section %}
    -i "{{ image_path }}" \
{% endif %}
    -d "{{ device }}" \
    -b "{{ batch }}" \
    -nstreams "{{ streams }}" \
    -t "{{ inference_time }}" \
    -hint none
