## Prepare Text Dataset For Benchmark

### Motivation

The Benchmark App can work with images or binary data. If there are no data to work with, benchmark app will create a random noise based on input datatype. To run benchmark on a project dataset, one will prepare a binary files for benchmark - one file per model input per sample - and pass an input file map `input_name_1:file_1,file_2,input_name_2:file_3` to the benchmark app.
