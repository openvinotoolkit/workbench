import { DropAreaDirective } from './drop-area.directive';

describe('DropAreaDirective', () => {
  it('should create an instance', () => {
    const directive = new DropAreaDirective();
    expect(directive).toBeTruthy();
  });
});
