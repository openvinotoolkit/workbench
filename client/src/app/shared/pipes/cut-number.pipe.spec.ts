import { CutNumberPipe } from './cut-number.pipe';

describe('CutNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new CutNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
