import { EstimatedTimePipe } from './estimated-time.pipe';

describe('EstimatedTimePipe', () => {
  it('create an instance', () => {
    const pipe = new EstimatedTimePipe();
    expect(pipe).toBeTruthy();
  });
});
