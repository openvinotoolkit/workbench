import { MultiLineTextPipe } from './multiline-text.pipe';

describe('MultiLineTextPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiLineTextPipe();
    expect(pipe).toBeTruthy();
  });
});
